# Remote BSP 待办

## 当前开发优先级

1. 运行时资源清单：把硬编码运动槽迁移为 EEPROM/Flash 配置、A/B 原子提交与回滚。
2. 智能步进运动：完善轨迹、跨板同步、限位实板验收和 TMC 并发。
3. 数字孪生：与实体资源清单共模，覆盖运动时间线、故障和确定性重放。
4. 遥测与监控：先稳定机器接口和指标语义，再制作趋势、告警和下钻界面。
5. 图形配置器：复用同一 schema、校验器和配置编译器。
6. 通用 SPI/I2C/ADC/Timer/Storage：待以上主线形成闭环后继续；PWM 与定时位流已进入实体波形验收阶段。

当前里程碑：Mock 可变轴数执行器、运动线协议、Linux C++ API/CLI、资源租约、
Classical CAN/CAN-FD 端到端调用、USB Vendor Bulk Linux主机与Mock全链路、
限位/队列欠载安全停机和第一版运动遥测已经
完成。通用 PWM 与定时位流已完成协议、Linux API/CLI、Mock、GUI 草案和三种
STM32 后端交叉编译。MCU无关的STEP/DIR/EN执行内核已从固定tick改为下一边沿TIM2 compare调度，
单轴/整板步频准入、脉宽/DIR时序、迟到停机、限位安全停机和运行指标已实现并测试。
F072/F103/G431均已交叉编译，原F072/F103 TIM3与G431 TIM4运动占用已经释放。
FLY-D5旧固定tick版本已完成五轴各 3200 步、
6 秒运动段的计数和自动禁用，以及运行中主动中止、队列欠载自动禁用验收。
compare版本尚未重新实板压力验收；也尚未完成轨迹规划、跨板时钟同步、分阶段组
提交、限位输入实板验收、运行时 NVM 引脚映射、F103/G431 的五轴实体压力验收和
TMC 并发事务。当前运动槽引脚和驱动器选择仍是编译期临时实现，不能作为最终
配置方式。

USB 第二传输第一阶段已完成 `LinkTransport`、逻辑路由、libusb 主机、RBU1 帧格式、
Mock 字节流、MCU 公共编解码器和 G431 Vendor Bulk Device 后端，USB APP 已成功
交叉编译。后续进行 G431 实体枚举、独立 APP PID、热插拔/重枚举、吞吐及断线故障
隔离验收；F072/F103 根据引脚及 F103 USB/CAN 共享 SRAM 约束分别适配，继续采用
编译期主链路二选一。以太网继续后置。

图形侧已完成第一版 RemoteBSP Studio：三块已知板卡的分组引脚选择、固定/重复
占用过滤、PWM/WS2812 参数草案、资源清单 JSON 导出，以及 Mock GPIO、步进位置、PWM、WS2812 演示和遥测
总览。Mock MCU 可选导出 100 ms 状态快照。当前 GUI 仍是只读观察与配置草案，
尚未接入 toolbusd 控制、正式资源 schema 编译和 NVM 部署事务。

兼容期 Kconfig 和 GUI 已支持显式共享 EN。MCU 公共层按物理引脚形成使能组并合并
各轴请求，避免一个轴结束时误关闭仍在运行的同组轴。正式运行时资源清单需要沿用
“EN 组引用”语义，并在部署前拒绝组内极性不一致或与其他资源复用。

## 运行时资源清单与持久化配置（最高优先级）

当前 `MOTION_SLOT0...` Kconfig 项仅用于验证板级后端和生成出厂默认布局；它们
必须迁移为版本化的运行时资源清单，不能继续决定某块工具板唯一的轴数、接线或
TMC 类型。

- `menuconfig` 只决定 MCU/板型、时钟、CAN、Katapult USB 恢复口占用、Flash 分区、是否链接运动/TMC
  后端，以及最大轴数、队列、端口等静态资源上限。
- EEPROM、内部 Flash 仿 EEPROM 或外部 FRAM 保存实际启用轴数、STEP/DIR/EN/限位
  引脚及极性、运动组、TMC UART/SPI 总线和地址、逻辑名称与安全初始状态。
- 为三块板的现有 Kconfig 映射生成“出厂默认资源清单 blob”；首次启动或两槽均
  无效时以它安全启动，所有输出保持关闭。
- 采用 A/B 槽、版本、长度、CRC-32、代数和提交标记；仅在停止所有相关运动组后
  写入非活动槽，校验通过后原子切换，掉电或校验失败回退旧槽。
- 加载和提交都校验板型/硬件 ID、引脚与复用、SWD/CAN/USB/晶振/Bootloader 保留
  资源、定时器/DMA/总线冲突及编译期资源上限。
- Mock 与实体 MCU 使用同一二进制格式、校验器和资源状态机；GUI、CLI 和生产工具
  只生成/部署清单，不直接依赖 Kconfig 槽位编号。

验收条件：同一已启用运动/TMC 功能的固件，不重新编译即可从 GUI 或 CLI 将 FLY-D5
从五轴改为两轴、重绑合法引脚并重启保持；无效或中断写入后仍回到最后一个有效且
安全关闭输出的配置。

串口里程碑：轮询和主动流式接收模式、MCU事件分批、toolbusd每对象64 KiB
缓冲、重复事件过滤、事件断档/缓冲溢出统计、C++完整写入及CLI端到端路径已经
完成，并通过Classical CAN和CAN-FD测试。后续还需按资源实施总线带宽配额、
实现G431实体UART HAL，以及上板进行持续GPS数据和8路并发压力测试。

## TMC2209 专用单线 UART

状态：F072/F103/G431 五端口后端、Linux TMC2209 编帧库、FLY-D5 五路实板读写和
五电机实际转动均已完成；F103/G431 的实体板验收、Mock 故障注入、非阻塞并发事务
与持续运动验收尚未完成。

新增独立任务：实现标准 RS-485 Modbus UART 适配器（RX、TX、DE/RE、3.5 字符帧间隔、
DMA/中断流式接收和每端口故障隔离）。它与 TMC2209 单线后端分层，不能复用为同一后端。

增加与智能运动模块相同原则的可选固件模块：

- TMC2209 驱动槽被选择时，自动链接固定 40000 baud 的内部单线 UART 后端；该
  后端不是通用串口资源，也不提供 Modbus 或 9600/115200 配置入口。
- 关闭后不链接执行代码，不分配事务状态、缓冲、定时器或 GPIO 资源。
- MCU 公共层实现 TMC2209 所需的定时收发状态机；F072、F103、G431 分别提供
  定时器和 GPIO 切换后端。未选择 TMC2209 驱动槽的板卡不链接该后端。
- 使用数量、引脚和定时器分配由板卡描述及运行时资源清单决定；波特率固定为
  TMC2209 所需的 40000 bit/s。

FLY-D5 只是该 TMC2209 通讯模块的一个五实例板级配置：

| 驱动槽 | 通信引脚 |
|---|---|
| D0 | PC13 |
| D1 | PC3 |
| D2 | PA3 |
| D3 | PA7 |
| D4 | PB1 |

- 同一引脚先作为输出发送，再切换为输入接收；支持起始位同步、定长接收、超时、
  总线忙和方向切换保护。
- 同一 MCU 上的 TMC2209 单线事务必须串行化或受明确的定时资源准入控制，不能影响
  STEP 定时器、CAN 心跳和安全输入处理。
- 当前 FLY-D5 第一版为安全起见：运动队列处于 `ARMED` 或 `RUNNING` 时拒绝新的
  TMC2209 写事务，因为现有后端仍会短时关中断。待改为非阻塞定时状态机后，才
  允许运动与 TMC 查询真正并发。
- PC13 按 STM32F072 的低速、低驱动能力约束配置，并在实体板上测量波形和时序
  裕量。
- 固件只提供 TMC2209 所需的单线半双工原子事务，不解析 TMC2209 寄存器、不包含型号专用
  驱动；数据报、CRC、IFCNT 写入确认、型号差异、重试和故障解释继续由 Linux
  TMC 库负责。
- FLY-D5 板卡描述把五个引脚声明为板级 TMC 通信资源；使用 TMC5160 时，同一
  插槽引脚可按板卡配置改为 SPI 片选，资源校验器必须拒绝两种模式同时占用。
- 增加 TMC2209 单线事务数、超时、采样错误、重试结果、最大定时延迟和 CPU 占用
  遥测。
- FLY-D5 已实板读取 TMC2209 `IOIN=0x21000041`，并通过 `IFCNT` 确认写帧被接收；
  接收尾部时序和写结果语义已经过实板修正。
- 后续在 Mock MCU 中实现 TMC2209 基础响应与故障注入；随后分别在 F103、G431 上
  验证板级后端，并使用 FLY-D5 和实体 TMC2209 验证五路独立读写
  及运动并发。

验收条件：同一套上层资源协议可在 F072、F103、G431 上工作；FLY-D5 五个槽位
均可读取 TMC2209 寄存器并通过 IFCNT 确认写入；持续运动、CAN 通信和 TMC
查询并发时不产生 STEP 抖动或节点掉线；关闭该功能后相关代码、状态和缓冲不
链接进固件。

## 通用可编程位流输出（WS2812 等）

状态：第一阶段已实现，实体波形和并发压力验收待完成。

已完成：

- 增加与具体灯珠型号无关的 `TIMED_BITSTREAM_CREATE/WRITE/ABORT` 协议、C++ API、
  CLI、请求去重和严格长度/时序校验。
- Linux `ws2812-create/ws2812-write` 包装器负责 RGB→GRB 和位流生成；MCU 不解析
  WS2812 颜色、动画或设备语义。
- Mock MCU 提供定时位流对象并把标准 WS2812 时序解码为逻辑像素，数字孪生可视化
  显示结果；Classical CAN 与 CAN-FD 端到端测试均已通过。
- F072、F103、G431 共用板级波形后端：TIM1_CH1/PA8 由 DMA 写 CCR；模块关闭时
  不编译实现、不分配对象表和静态 DMA 缓冲。三种正式板卡配置均已交叉编译。
- GUI 可选择 PA8、设置灯珠数量与预览颜色，导出 `timed_bitstream` 资源草案，
  并与 GPIO、运动和 PWM 做引脚冲突检查。

尚需完成：示波器核对 T0H/T0L/T1H/T1L/reset，实体灯带颜色与长度测试；运动、
CAN 满载时的 DMA/中断延迟测试；完成事件、超时/欠载遥测；将编译期默认映射迁移
到正式运行时资源清单；压缩 F072 静态波形缓冲占用。

增加独立于具体 LED 型号的可选定时位流输出模块：

- `CONFIG_REMOTEBSP_TIMED_BITSTREAM`：是否编译定时位流输出，默认关闭。
- 关闭后不链接波形执行器，不分配像素/位流缓冲、DMA、定时器或 GPIO 资源。
- Linux WS2812 库负责 RGB/GRB 顺序、亮度、像素布局、动画和设备语义；MCU
  只接收打包位流以及 `T0H/T0L/T1H/T1L/reset` 时序参数，并执行一次原子波形
  输出，因此 MCU 中不加入 WS2812 专用设备驱动。
- F072、F103、G431 优先采用定时器 PWM + DMA 后端；没有合适 DMA 映射时，
  只有通过最坏时延验证的定时器后端才能启用，禁止普通忙等待长期关中断。
- 通道数、最大位流长度、双缓冲、GPIO、定时器和 DMA 映射由板卡能力与运行时
  资源清单决定，不能按固定灯珠数量编译公共协议。
- 输出前执行定时器/DMA/带宽准入检查；WS2812 刷新属于低优先级业务，不得阻塞
  CAN 心跳、安全事件、STEP 输出和跨板时钟同步。
- 支持完成事件、忙、取消、超时、欠载、实际刷新耗时、最大 ISR/DMA 延迟和
  CPU 占用遥测。
- Mock MCU 支持确定性记录位流、解码为逻辑像素供数字孪生显示，并可注入中断
  延迟、欠载和资源冲突。

验收条件：同一主机 API 可驱动 F072、F103、G431 上的 WS2812 灯带；运动和
CAN 满载测试中波形满足配置时序且不影响安全与 STEP 实时性；不同颜色顺序和
灯珠数量只修改 Linux/运行时配置；关闭模块后固件不保留相关执行代码和静态
缓冲。

## 通用 PWM

状态：第一阶段已实现，三块板的新通用接口待实板验收。

- 增加 `PWM_CREATE/PWM_WRITE/PWM_STOP` 协议、C++ API、CLI、Mock BSP 和数字孪生。
- 频率使用 Hz，占空比使用 0～10000 万分比，支持高/低有效极性；写入只改变
  占空比，停止后输出回到非有效电平。
- F072/F103 使用 PA6/TIM3_CH1，G431 使用 PC6/TIM3_CH1；三者均已交叉编译。
- GUI 可设置启用、引脚、频率、占空比和极性，并显示表盘式运行预览。
- 后续使用示波器验证频率、占空比、极性、动态更新和停止安全电平，再把引脚与
  频率上限改由运行时资源清单和板卡能力合同决定。

## 高优先级架构基础

以下五项是智能运动、跨板同步和图形配置器大规模实现前的公共基础。

### 资源能力合同与准入控制

状态：Mock第一阶段已实现，准入调度和实体能力标定待完成。

- 节点除资源数量外，还要报告定时器分辨率、最大持续步频、最大同时轴数、
  队列容量、缓冲容量、吞吐和最坏延迟等可保证指标。
- 能力描述必须反映当前固件裁剪、运行时配置和已经占用的底层资源。
- Linux 在创建资源或启动运动前执行准入检查，不能满足实时预算时提前拒绝。
- 能力值必须来自实体基准测试，区分理论上限、实测上限和正式保证值。

验收条件：主机可以在发送运动段之前判断节点是否满足轴数、步频、队列和同步
精度要求，并对不满足的配置给出明确原因。

当前进展：协议、Mock查询、C++ API、CLI和编解码测试已完成。合同已覆盖
GPIO/UART和Mock STEPGEN轴，运动轴声明100 kstep/s、2 µs脉宽、32段队列；
这些仅是流程测试值。主机自动准入以及F072/F103/G431实体保证值尚未实现。

### 资源租约与所有权

状态：远端会话级第一阶段已实现，本地应用强所有权待完成。

- 增加资源获取、续租、释放和租约超时机制。
- 运动轴、运动组、UART和SPI默认独占；只读输入允许按策略共享订阅。
- 租约绑定`toolbusd`会话和客户端身份，禁止其他客户端绕过所有权操作资源。
- 客户端退出、会话重启或租约超时后自动释放，并执行资源安全状态。
- 节点离线再上线时不得恢复旧会话租约。

验收条件：两个客户端竞争同一输出资源时只有一个成功；持有者异常退出后资源
在限定时间内进入安全状态并可被新客户端重新获取。

当前进展：Mock支持共享读、独占、续租、释放、状态、租约到期和会话清理；
关联GPIO在释放时拉低，UART执行资源复位，运动租约释放会中止已武装或运行的
运动。由于本地短连接共享`toolbusd`远端
会话，仍需持久IPC客户端身份或逐操作租约令牌才能满足完整验收条件。

### CAN业务优先级与带宽准入

状态：线时间估算、分类和发送前准入第一阶段已实现，优先级队列待完成。

建议优先级由高到低：

1. 急停、限位和严重故障。
2. 时钟同步、运动段和队列告警。
3. 心跳及节点状态。
4. 普通GPIO/UART/SPI请求。
5. 遥测和持续UART数据。
6. 固件升级、配置写入等大块传输。

- `toolbusd`按优先级和每资源配额调度，控制短请求不能被大块数据长期阻塞。
- 运动组启动前预留所需总线带宽，不能满足时拒绝启动。
- 低优先级数据支持限速、分块和拥塞丢弃，安全事件不得丢弃。
- Classical CAN与CAN-FD分别建立准确的线占用估算模型。

验收条件：在持续UART和低优先级大包压满允许配额时，心跳、限位事件和运动段
仍能满足规定延迟，并能报告带宽拒绝原因。

当前进展：`toolbusd`已建立安全、运动、系统、交互、持续流和大块传输六类业务，
使用全局与分类令牌桶进行发送前准入。Classical CAN按11位标准帧和最坏位填充
上界估算；CAN-FD按实际DLC补齐、仲裁/数据双速率和BRS估算。CLI/API可查询
累计准入、拒绝、保证发送、帧数和估算线时间；低预算端到端测试已验证超预算
大请求被立即拒绝且不会影响后续小请求。尚未实现按优先级排队、每节点/资源
配额、接收方向负载、运动带宽预留和实体总线标定。

### 统一板卡描述包

状态：Mock第一阶段已实现，实体MCU/核心板完整描述待完成。

为每个MCU和核心板建立唯一的机器可读描述，至少包含：

- 芯片引脚、封装键合和复用功能。
- 定时器、DMA、UART、SPI、ADC和CAN映射。
- Flash/RAM布局、擦除粒度和配置槽约束。
- SWD、晶振、CAN、USB、Bootloader、硬件ID等保留资源。
- 板载LED、按键、收发器控制和输出安全默认电平。

`menuconfig`约束、固件资源校验器、图形配置器、接线表和文档必须从同一描述包
读取或生成，禁止分别手工维护相互矛盾的引脚表。

验收条件：修改一项板卡引脚定义后，固件检查、GUI可选项和生成接线表同步变化；
保留引脚在主机和MCU两侧都会被拒绝。

当前进展：已建立严格JSON schema v1、默认Mock描述和FLY-D5五轴数字孪生描述，
板型、UUID模板、固件版本、
能力、公开资源、能力合同和内部保留资源均由描述加载。加载器拒绝未知字段、
重复ID、能力不匹配及“既公开又内部占用”等冲突。FLY-D5已经接入MCU选择、
Flash/RAM布局、CAN/USB/SWD保留引脚、构建脚本和中文接线文档；五轴物理引脚
尚未由同一JSON自动生成固件配置。F103/G431完整引脚复用、DMA、固件生成器和
接线表仍待接入。

STM32F072RBT6已经拆分为通用MCU层和可选板型层：通用层支持两组CAN、两组
USART1、GPIO、独立/Katapult APP布局；FLY-D5只提供板级覆盖。新增其他F072
板卡时应复用通用层，禁止再复制一份协议核心或芯片HAL实现。

### 数字孪生Mock工具板

状态：基础资源与故障注入第一阶段已实现。

- Mock MCU直接加载与实体节点相同的运行时资源清单。
- 模拟STEP/DIR/EN、限位、按钮、UART/SPI事务和TMC基础响应。
- 支持注入晶振漂移、CAN延迟/丢包、队列欠载、资源卡死、节点掉线和重启。
- 导出带全局时间和本地tick的STEP边沿，用于验证同板与跨板同步。
- 支持记录真实`toolbusd`会话并在Mock环境中确定性重放。

验收条件：一份图形配置器工程可以同时部署到Mock和兼容实体板；固定故障脚本
重复运行能得到一致的状态转换、错误码和安全停止结果。

当前进展：Mock已直接加载版本化板卡资源清单；轴数由每块板的资源描述和能力
合同决定，默认示例为3轴，单节点v1线格式最多64轴。执行器会导出全局纳秒时间、
本地tick、STEP/DIR/EN边沿，并支持运动限位故障、队列欠载和安全停机。故障脚本
还支持单路UART故障、GPIO输入以及节点离线/恢复。TMC、时钟漂移、CAN丢包、
重启和真实会话重放尚未实现。

## 图形化板卡资源配置器

状态：本地 GUI 第一版已经实现板卡选择、运动轴映射、DIR 反相、共享 EN、冲突
过滤、资源清单 JSON 导出以及 Mock 数字孪生；正式资源 schema、实体节点部署和
GPIO 板级接口约束仍在推进。

开发一个类似 STM32CubeMX 的图形化配置工具，用于管理工具板 EEPROM、内部
Flash 仿 EEPROM 或外部 FRAM 中的运行时资源清单。该工具不是固件
`menuconfig` 的替代品：

- `menuconfig` 继续管理 MCU、晶振、CAN、Katapult USB 恢复口占用、Bootloader 布局和功能模块裁剪。
- 图形化配置器管理 STEP/DIR/EN、限位、按钮、TMC UART/SPI、逻辑资源名称和
  资源依赖等运行时参数。

计划能力：

- 按 MCU/板型显示可用引脚、复用功能、定时器通道、UART、SPI、ADC 和保留资源。
- 图形化创建 GPIO、UART、SPI、步进轴、运动组、限位、按钮和 TMC 连接。
- GPIO 按固定占用、板级定向接口和真正通用引脚三类建模。板级定向接口由原理图
  锁定或限制方向、外部上下拉、有效电平和安全状态；只有通用引脚允许自由选择
  输入/输出、内部上下拉、输出初值、输入消抖和事件策略。
- 实时检查引脚复用、定时器通道、总线、片选、DMA 和板级内部占用冲突。
- 明确标出 SWD、CAN、USB、晶振、Bootloader 和硬件 ID 等禁止覆盖的资源。
- 自动检查跨板运动组成员、时钟同步能力、安全域和限位位置是否合理。
- 从在线节点读取当前配置和 A/B 槽状态，支持备份、比较和恢复。
- 提供在线监控页，显示 MCU/`toolbusd` CPU 占用、定时中断耗时、运动队列
  余量、栈水位、CAN 总线负载、错误计数和节点同步精度。
- 支持趋势曲线、峰值锁存、阈值告警和按节点/资源下钻，明确区分估算值与硬件
  实测值。
- 保存人类可读的工程文件，并编译为 MCU 使用的版本化二进制资源清单。
- 在下发前完成与 MCU 相同的静态校验，显示具体冲突位置和修改建议。
- 通过 `CONFIG_STAGE_*`、`CONFIG_VALIDATE`、`CONFIG_COMMIT` 和
  `CONFIG_ROLLBACK` 完成分阶段写入、原子提交及回滚。
- 配置版本升级时提供 schema 迁移和兼容性提示。
- 生成配置摘要、资源占用表、接线表和可纳入 Git 的差异。
- 默认提供中文界面，并支持 Windows 和 Linux 主机。

验收条件：

1. 不重新编译固件即可修改一块板的步进电机和 TMC 接线。
2. 冲突配置无法提交，并能定位到具体资源。
3. 写入或掉电中断后，节点仍能启动最后一个有效配置。
4. 可以从空白节点完成读取、编辑、校验、提交、重启验证和回滚闭环。
5. 配置工程文件可重复生成完全相同的二进制清单。

界面技术栈和工程文件格式在实现阶段决定；底层资源 schema、校验器和配置编译器
必须与 GUI 解耦，以便命令行、自动化测试和批量生产工具复用。
